package com.mobdeve_group45_mco.hourlyWeather

class HourlyWeather(hour: String, iconId: Int, temp: String) {
    var hour= hour
        private set

    var iconId = iconId
        private set

    var temp = temp
        private set
}